# 此目录存放 sherpa-onnx AAR
#
# 下载地址：https://github.com/k2-fsa/sherpa-onnx/releases
# 文件名：sherpa-onnx-1.12.14.aar（选最新稳定版，勿选 -rknn 变体）
#
# 下载后放入此目录，然后：
#   git add android/app/libs/sherpa-onnx-1.12.14.aar
#   git commit -m "chore: add sherpa-onnx AAR"
#
# CI 若发现此目录无 AAR，会自动从 GitHub Releases 下载。
# 但本地构建必须手动下载（国内浏览器可访问 GitHub Releases 页面）。
