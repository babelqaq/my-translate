# 项目总体规划：my_translate 双语实时对话翻译器

> 版本：v1.1（2026-07-29。v1.0→v1.1 修订：① 模式 A 也提供手动语种 Chip；② 新增"外部下载资源清单"置顶——AAR 走 GitHub Releases 直下入 `libs/` 并入库，模型一律走 ModelScope 镜像，**不从 GitHub / Maven 拉取（国内网络必然失败）**）
> 性质：**唯一的编码指导文档**。后续所有代码修改以本文件为准；与旧文档冲突时以本文件为准。
> 配套文档：`Project Review.md`（问题编号 S/M/L 的出处）、`整体重构方案_SherpaONNX.md`（评审历史与决策依据）

---

## 〇、外部下载资源清单（置顶，动码前先备齐）

> 铁律：**开发/构建环境不得尝试从 GitHub Releases 或 Maven Central 在线拉取模型与 AAR（国内网络必然失败）**。下表为唯一认可的获取方式；所有资源由用户手动下载一次后，AAR 提交入 git，模型放本地/CI 缓存。

| # | 资源 | 用途 | 下载地址 | 放置位置 | 是否入 git |
|---|------|------|----------|----------|-----------|
| 1 | `sherpa-onnx-1.12.x.aar`（≈38–54MB，含 arm64 so + Kotlin API） | Kotlin ASR 底座 | GitHub Releases（唯一走 GitHub 的资源，浏览器直下）：<br>`https://github.com/k2-fsa/sherpa-onnx/releases`<br>资产名形如 `sherpa-onnx-1.12.25.aar`（选最新稳定版，勿选 `-rknn` 变体） | `android/app/libs/` | **是**（<100MB，一次性入库，CI 无需再下载） |
| 2 | `silero_vad.onnx`（~2MB） | VAD | ModelScope 全量镜像仓：<br>`https://modelscope.cn/models/zhaochaoqun/sherpa-onnx-asr-models`（模型文件页内下载） | `android/app/src/main/assets/models/vad/` | 否（git 忽略） |
| 3 | `sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20`（int8 四件套 **~190MB**；官方压缩包 ~488MB 属正常——内含 fp32 315M + int8 174M + 测试音频，只提取 int8） | 模式 A 唯一识别器 + 模式 B 中文侧 | ModelScope 官方同名镜像：<br>`https://modelscope.cn/models/pkufool/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20`<br>取 `encoder/decoder/joiner` 的 **int8** onnx + `tokens.txt` | `android/app/src/main/assets/models/zh_en/` | 否（git 忽略） |
| 4 | `sherpa-onnx-streaming-t-one-russian-2025-09-08`（CTC；压缩包 ~123MB，**仅含 fp32 `model.onnx` ~138MB + tokens.txt，官方无 int8**，2026-07 实测） | 模式 B 俄语识别器 | ModelScope 全量镜像仓（同 #2，160+ 模型定期同步）：<br>`https://modelscope.cn/models/zhaochaoqun/sherpa-onnx-asr-models`<br>在文件页内检索 `t-one-russian`；若该镜像未同步，则在 ModelScope 站内搜索模型名，以实际镜像仓为准 | `android/app/src/main/assets/models/ru/` | 否（git 忽略） |

- **2026-07-31 修订（INV-MIRROR 范围收窄）**：`tools/fetch_models.sh` 中的 ModelScope 推测直链实测 404（CI run #16 失败），**已弃用**（脚本保留但入口报错退出）。现行唯一下载路径：`tools/fetch_models_github.sh`（GitHub 官方 Releases）——**CI 海外 runner 直连可用（已实测），本地需开 VPN（已实测）**。上表 #2–#4 的 ModelScope 地址仅作应急人工检索线索，使用前必须先 curl 验证。
- CI 对 `assets/models/` 做 actions/cache（key = 下载脚本 hash），二次构建免下载。

---

## 目录

0. 外部下载资源清单（置顶）
1. 项目定位、目标与原则
2. 现状盘点（改造起点）
3. 目标架构总览
4. 模型选型与资产策略
5. 目标目录结构
6. Android Kotlin 层详细设计
7. Dart 层详细设计
8. 关键数据流时序
9. 构建、CI 与工具链
10. Phase A/B/C/D 实施计划（文件级任务分解）
11. 不变量清单（INV-*，任何修改不得违反）
12. 风险与回退预案
13. 附录（Prompt / 事件协议 / 参数表）

---

## 一、项目定位、目标与原则

### 1.1 定位

**双语言实时对话翻译器（Bilingual Conversation Translator）**——个人自用、免费、离线 ASR + 在线 LLM 翻译的 Android App。目标体验对标 ChatGPT Voice 的双语模式。

两种使用模式（无第三种）：

| 模式 | 场景 | 行为 |
|------|------|------|
| A：中文 ⇄ 英语（默认） | 与英语使用者面对面交流 | 对方说英语 → 中文大字幕（静音）；你说中文 → 手机朗读英语译文 |
| B：中文 ⇄ 俄语 | 与俄语使用者面对面交流 | 对方说俄语 → 中文大字幕（静音）；你说中文 → 手机朗读俄语译文 |

### 1.2 优先级四原则（冲突时永远偏向前者）

```
① 翻译准确率  >  ② 流式体验  >  ③ 实时响应  >  ④ 代码简洁
```

### 1.3 核心技术决策（已确认，不再讨论）

| # | 决策 |
|---|------|
| D1 | 只做 中⇄英 / 中⇄俄 两种模式；不做英⇄俄、不做三语自动识别 |
| D2 | ASR 全面转向 Sherpa-ONNX（官方预编译底座 + 自写 Kotlin 调度层）；移除 Vosk / Google speech_to_text / flutter_sound |
| D3 | 语言判定极简化：模式 A 用 bilingual 单模型 + Dart 字符集定方向；模式 B 单识别器 sticky + 字符校验兜底。**两种模式均提供手动语种 Chip 覆盖（手动 > 自动，永远优先）**。无 logistic 标定、无双识别器常驻竞争、无锁定状态机 |
| D4 | 模型全部打进 APK，运行期全离线（目标设备不联网，无法现场下载） |
| D5 | 翻译保留多供应商（GLM/Qwen/Doubao，OpenAI 兼容）+ 流式开关；SSE 默认开，首包 3s 超时自动降级非流式 |
| D6 | 分阶段落地：Phase A/B/C 必做、D 可选；每阶段独立可运行、完成即停等确认 |
| D7 | Kotlin/Dart 职责边界：**Kotlin 只管"听"**（采集/VAD/推理）；Dart 管 session/语义路由/翻译/TTS/UI |

---

## 二、现状盘点（改造起点）

### 2.1 现有文件与去向

| 现有文件 | 现职责 | 去向 |
|----------|--------|------|
| `lib/main.dart` | 入口，Provider 装配 | 保留，微调（注册新服务） |
| `lib/app.dart` | MaterialApp | 保留 |
| `lib/pages/live_page.dart` | 主页（Selector 局部刷新已完成） | 重写 UI 结构（模式卡片/预览区/Chip），保留 Selector 模式 |
| `lib/pages/settings_page.dart` | 设置页 | 改造（删引擎选择，加流式开关） |
| `lib/services/live_session.dart` | 会话编排（Vosk 双模型仲裁/缓冲/防抖） | 大幅精简重写 |
| `lib/services/app_settings.dart` | 设置持久化 | 改造（字段增删，见 7.6） |
| `lib/services/translation_service.dart` | 非流式 LLM 翻译 + 独立标点请求 | 改造（`translate(source,target,text)` + `translateStream`；删 `punctuate`） |
| `lib/services/tts_service.dart` | flutter_tts 封装 | 改造（TtsQueue + SentenceSegmenter） |
| `lib/services/speech/speech_engine.dart` | 引擎抽象 | 重写为 `NativeAsrEngine`（Channel 封装） |
| `lib/services/speech/vosk_engine.dart` | Vosk 双模型引擎 | **删除** |
| `lib/services/speech/google_engine.dart` | Google 在线引擎 | **删除** |
| `assets/*.zip`（3 个 Vosk 模型，~130MB） | 离线模型 | **删除**（换 sherpa 模型，见第四节） |
| `pubspec.yaml` | 锁死 Dart 2.19 | 解锁 Dart 3（见 9.1） |
| `codemagic.yaml`、`.github/workflows/` | CI（每次 `flutter create` 生成平台工程） | 重写（平台工程入库后直接 build，见 9.3） |
| `android/`（**不存在**） | — | **新建并入库**（含 Kotlin 层，不能再靠 CI 临时生成） |

### 2.2 Phase 1（已完成，成果必须保留）

已修复：S5 CI 版本冲突、S7 stop() 重朗读、M1/M2 UI 重建收窄（Selector）、M3 超时+连接复用、M6 状态枚举化（`SessionStatus`）、M7 俄语手动 Chip、M10 中文早提交阈值；已清理死代码 L1–L9。

### 2.3 遗留问题与解决阶段对照

| 问题（Project Review.md 编号） | 解决于 |
|------|--------|
| S1 非流式翻译 / S2 串行双 LLM / S6 `_runPunct` race | Phase B |
| S3 ASR 占 UI 线程 | Phase A（下沉 Kotlin 原生线程） |
| S4 TTS 阻塞 | Phase C |
| M4/M5 语种仲裁散乱、缓冲复杂 | Phase A 消亡（模式 A 无仲裁）+ Phase D（模式 B 极简 sticky） |

---

## 三、目标架构总览

```
┌──────────────────── Flutter (Dart：session/翻译/TTS/UI) ────────────────────┐
│                                                                            │
│  LivePage（模式卡片 / "正在听"预览 / 中文大字幕 / 同传气泡 / 手动 Chip）        │
│      ▲ Selector 局部刷新（Phase 1 成果）                                     │
│  LiveSession（会话编排，ChangeNotifier）                                     │
│   ├── partial → 仅更新"正在识别"预览（永不进翻译）            [INV-PARTIAL]   │
│   ├── final {text} → TranslationRouter.route(text, mode, manualLang)       │
│   │        手动 Chip 优先定 source；Chip=自动 时字符集统计定 source           │
│   │        （CJK→zh / Latin→en / Cyrillic→ru）                              │
│   │        模式表定 target（zh⇄en：zh→en, en→zh；zh⇄ru 同理）                │
│   │        └→ TranslationService.translateStream(source, target, text)     │
│   │              ├ stream=true  → SSE delta → 字幕逐字浮现                   │
│   │              │       └→ [仅 source=zh] SentenceSegmenter → TtsQueue     │
│   │              └ 降级 stream=false → 整句返回 → [仅 source=zh] TtsQueue    │
│   ├── 非对称 TTS [INV-TTS]：source=zh → 朗读外语译文（手机替你说）             │
│   │                        source=en/ru → 仅中文大字幕，静音（你看字）        │
│   └── TtsQueue → flutter_tts，按 target 选 en-US / ru-RU（zh-CN 永不出现）   │
│      ▲                                                                     │
│      │ EventChannel "asr/events" 上行（JSON，见附录 13.2）                   │
│      │ MethodChannel "asr/control" 下行：start(mode)/stop/setActiveLang/…   │
└──────┼──────────────────────────────────────────────────────────────────────┘
       │
┌──────┴───────────────── Android Kotlin（只做"听"）──────────────────────────┐
│  AsrPlugin（Channel 注册与消息编解码，唯一 Flutter 接触点）                    │
│  AudioSessionManager（生命周期/权限/幂等 start-stop/HandlerThread 总控）      │
│   ├── AudioCapture      AudioRecord → PCM16 mono 16kHz，100ms 帧            │
│   ├── VadProcessor      Silero VAD：speechStart/End 切句、静音不喂 ASR        │
│   │        └── SegmentBuffer：环形缓存当前 VAD 段 PCM ≤15s（仅模式 B）        │
│   ├── RecognizerManager 按模式加载识别器；sticky 单活跃喂帧；                  │
│   │        模式 A：bilingual-zh-en ×1（无切换问题）                          │
│   │        模式 B：{bilingual, ru} 双加载单活跃 + 字符校验兜底重解码           │
│   └── SherpaRecognizer  单识别器包装：feed/partial/final/reset               │
└─────────────────────────────────────────────────────────────────────────────┘
```

线程模型：
- **audio 线程**（AudioRecord 读循环）→ 帧入队；
- **inference HandlerThread**（唯一推理线程）：VAD + ASR 串行执行（int8 每帧 decode ~10ms ≪ 100ms 帧间隔，无需多线程锁）；
- 事件经 `Handler(Looper.getMainLooper())` 抛回主线程再进 EventChannel（Flutter 平台通道要求主线程）。

---

## 四、模型选型与资产策略

### 4.1 模型清单（全 App 共 3 个文件，2 个 ASR 模型）

| 用途 | 模型（sherpa-onnx 官方发布） | 体积(int8) | 备注 |
|------|------|-----------|------|
| VAD | `silero_vad.onnx` | ~2MB | 两模式共用 |
| 中文+英文 ASR | `sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20`（int8 encoder/decoder/joiner + tokens.txt） | **~190MB**（2026-07 官方文档实测 encoder 174M + decoder 13M + joiner 3.1M；早期 ~80MB 估算有误，用户已确认选标准版） | 模式 A 唯一识别器；模式 B 的 zh 侧复用；**专为 code-switch 训练**。降级备选：`small-bilingual-zh-en-2023-02-16`（int8 ~48MB，同架构、准确率略低），真机验证标准版不可用时启用 |
| 俄语 ASR | `sherpa-onnx-streaming-t-one-russian-2025-09-08`（CTC） | **~138MB（仅 fp32 `model.onnx`，官方未提供 int8；2026-07 实测压缩包 123MB）** | 仅模式 B 加载；代码按"目录内首个 .onnx"加载，兼容 fp32 |

### 4.2 打包与加载

- 存放：`android/app/src/main/assets/models/{vad,zh_en,ru}/...`，Kotlin 经 `AssetManager` 直读（sherpa-onnx Kotlin API 原生支持 asset 路径）。
- Gradle：`androidResources { noCompress += "onnx" }`（旧写法 `aaptOptions.noCompress "onnx"`）；`ndk { abiFilters += "arm64-v8a" }` 单架构。
- **模型文件不入 git**（bilingual 接近 GitHub 100MB 单文件红线，且二进制入库无意义）：本地与 CI 统一跑 `tools/fetch_models_github.sh`（GitHub 官方源；CI 直连、本地开 VPN——2026-07-31 修订，原 ModelScope 版 fetch_models.sh 因直链 404 已弃用）。`.gitignore` 加 `android/app/src/main/assets/models/`。
- **加载策略**：进入模式一次性加载该模式全部模型 → memory resident → 退出/切模式才释放。绝不逐句换载。
- 删除现有"首启解压 Vosk zip"逻辑与 `assets/*.zip`。
- APK 预计 **~370–390MB**（2026-07 二次修正：bilingual int8 ~190MB + 俄语 T-one 仅有 fp32 ~138MB，官方无 int8 版）；内存：模式 A 常驻 ~190MB 权重、模式 B ~330MB 权重（**推理只有一份**）。若体积/内存不可接受：① bilingual 降级 small 版（int8 ~48MB）；② 俄语 fp32 可自行用 onnxruntime 动态量化出 int8（效果需真机验证），或俄语模型改为按需下载不打包。

### 4.3 Phase A 首要验证点（本方案基石）

Phase A 第一件事：真机实测 bilingual-zh-en 三种输入——纯中文、纯英文、中英混说（"我要 book 一个 hotel"）。
- 达标（主观：识别可用、不明显低于 Vosk）→ 按本规划继续。
- 不达标 → 备选：模式 A 退回 zh + en 双 mono 模型 + 模式 B 同构的 sticky 兜底（代码完全复用），**不回退到标定竞争**。

---

## 五、目标目录结构

```
E:\MY_TANSLATE\
├── android/                                # 平台工程【入库，不再由 CI 生成】
│   └── app/
│       ├── build.gradle                    # minSdk 23 / abiFilters / noCompress onnx / sherpa 依赖
│       ├── proguard-rules.pro
│       └── src/main/
│           ├── AndroidManifest.xml         # RECORD_AUDIO / INTERNET
│           ├── assets/models/              # 模型（git 忽略，脚本下载）
│           │   ├── vad/silero_vad.onnx
│           │   ├── zh_en/{encoder,decoder,joiner}.int8.onnx + tokens.txt
│           │   └── ru/{model}.int8.onnx + tokens.txt
│           └── kotlin/com/example/my_translate/
│               ├── MainActivity.kt
│               └── asr/
│                   ├── AsrPlugin.kt
│                   ├── AudioSessionManager.kt
│                   ├── AudioCapture.kt
│                   ├── VadProcessor.kt
│                   ├── RecognizerManager.kt
│                   ├── SherpaRecognizer.kt
│                   └── AsrConfig.kt
├── lib/
│   ├── main.dart
│   ├── app.dart
│   ├── pages/
│   │   ├── live_page.dart
│   │   └── settings_page.dart
│   └── services/
│       ├── live_session.dart               # 会话编排 + INV-TTS 过滤点
│       ├── translation_router.dart         # 新增：字符集判 source + 模式表定 target
│       ├── translation_service.dart        # translate + translateStream(SSE)
│       ├── tts_service.dart                # TtsQueue
│       ├── sentence_segmenter.dart         # 新增：流式 delta → 句子
│       ├── app_settings.dart
│       └── speech/
│           └── native_asr_engine.dart      # 新增：Channel 封装（替代原 speech/ 三文件）
├── tools/
│   └── fetch_models.sh                     # 下载 sherpa 模型到 assets
├── pubspec.yaml                            # Dart 3
├── codemagic.yaml / .github/workflows/     # CI：fetch_models + build
├── BUILD.md                                # 同步更新
└── 项目总体规划.md（本文件）
```

---

## 六、Android Kotlin 层详细设计

依赖底座：**GitHub Releases 直下官方 AAR（`sherpa-onnx-1.12.x.aar`，含 arm64 so 与 Kotlin API）放 `android/app/libs/` 并提交入 git**——这是唯一路径，**不配置 Maven 坐标**（国内拉取必失败，且入库后 CI 也无需网络）。下载地址见文档开头〇节清单 #1。

### 6.1 `AsrPlugin.kt` — Channel 边界（唯一 Flutter 接触点）

```kotlin
// MethodChannel "asr/control"（下行）
//   start(mode: "zhEn"|"zhRu")  -> 幂等；已在跑则先 stop
//   stop()
//   setActiveLang(lang: "zh"|"ru"|"auto")   // 仅模式 B 有意义
//   setConfig(json: String)                  // AsrConfig 热更新
// EventChannel "asr/events"（上行）JSON 见附录 13.2
class AsrPlugin(...) : MethodChannel.MethodCallHandler, EventChannel.StreamHandler {
    private val sessionManager = AudioSessionManager(context, ::emit)
    private fun emit(event: Map<String, Any?>) { /* mainHandler.post { sink?.success(json) } */ }
}
```

要点：在 `MainActivity.configureFlutterEngine` 注册；所有 emit 必须切主线程；`onCancel` 时只断 sink 不停会话（防止热重载误停）。

### 6.2 `AudioSessionManager.kt` — 生命周期总控

- 职责：幂等 start/stop、HandlerThread（`inference`）创建与销毁、异常捕获重启（推理抛异常 → emit error → 自动 stop）、Activity 生命周期联动（onPause 不停止——用户可能锁屏使用；onDestroy 必停）。
- `start(mode)` 流程：校验权限（权限申请仍在 Dart 侧 permission_handler，Kotlin 只做兜底校验）→ RecognizerManager.load(mode)（在 inference 线程，加载完 emit `ready`）→ AudioCapture.start()。
- `stop()` 流程：AudioCapture.stop() → 冲刷 VAD 尾段 → RecognizerManager 出最后 final → 释放识别器（切模式时）。

### 6.3 `AudioCapture.kt` — 采集

- `AudioRecord`，`MediaRecorder.AudioSource.VOICE_RECOGNITION`，16000Hz / MONO / PCM_16BIT。
- 独立 audio 线程 read 循环，每 **1600 samples（100ms）** 一帧，`FloatArray`（/32768f 归一化，sherpa 接口吃 float）投递到 inference 线程。
- bufferSize = `max(minBufferSize, 4 * frameBytes)`；read 返回负值 → emit error。

### 6.4 `VadProcessor.kt` — 切句与门控

- sherpa-onnx `Vad` + `SileroVadModelConfig`（threshold=0.5，minSilenceDuration=0.8s，minSpeechDuration=0.25s，初值进 AsrConfig）。
- 状态机：`silence → speaking`（emit `vadStart`）→ `silence`（emit `vadEnd`，通知 RecognizerManager 取 final）。
- **静音期不喂 ASR**（省电核心）；speaking 期把帧转发 RecognizerManager，同时（仅模式 B）写入 `SegmentBuffer`（环形 FloatArray，容量 15s×16000，段结束由 RecognizerManager 决定清空或用于兜底重解码）。

### 6.5 `RecognizerManager.kt` — 识别器持有与 sticky 调度

```kotlin
class RecognizerManager(assets: AssetManager, cfg: AsrConfig, emit: (Map<String,Any?>)->Unit) {
    // load(mode): zhEn -> [bilingual]; zhRu -> [bilingual(作zh), ru] 双加载
    // activeLang: 模式A恒 "auto"（单识别器）；模式B "zh"|"ru"，默认上次使用值
    // onFrame(samples): 只喂 active 识别器；有 partial 变化即 emit partial
    // onSegmentEnd(): active.final -> 模式A直接 emit final
    //                 模式B: charMatch(final, activeLang)?
    //                   yes -> emit final; SegmentBuffer.clear()
    //                   no  -> 另一识别器对 SegmentBuffer 重解码一次（Phase D 才启用；
    //                          Phase A 阶段 charMatch 失败也直接上抛，靠手动 Chip）
    //                          比较两侧字符集匹配度择优 -> emit final(+langSwitched) -> 切 activeLang
    // setActiveLang(lang): 手动 Chip -> 强制切换 + reset 两识别器 + 清 SegmentBuffer
}
```

- 字符校验是**机械规则**（字符集统计 + 择优），不含语义策略，不违反 D7 边界。
- 极短词/数字等字符集模糊时用两侧 raw confidence 粗排辅助，**不标定**（不同架构分数仅在"文本无法判别"的兜底场景做粗略参考）。

### 6.6 `SherpaRecognizer.kt` — 单识别器包装

- 包装 `OnlineRecognizer`（bilingual zipformer transducer）与俄语 CTC 模型的差异（构造 config 不同，对外统一 `feed(samples)` / `partialText` / `finalizeSegment(): Result(text, confidence)` / `reset()`）。
- `decodeBuffer(samples: FloatArray): Result` —— 供兜底重解码：新建 stream、整段喂入、出结果（int8 解码 ≤15s 音频约几百 ms，可接受）。

### 6.7 `AsrConfig.kt` — 参数集中地（全部可由 Dart setConfig 热更）

见附录 13.3 参数表。

---

## 七、Dart 层详细设计

### 7.1 `services/speech/native_asr_engine.dart`（新增）

```dart
class AsrEvent { final String type; final String text; final String? langSwitched; }

class NativeAsrEngine {
  static const _control = MethodChannel('asr/control');
  static const _events = EventChannel('asr/events');
  Stream<AsrEvent> get events;                 // broadcast，JSON 解码
  Future<void> start(String mode);             // 'zhEn' | 'zhRu'
  Future<void> stop();
  Future<void> setActiveLang(String lang);     // 'zh'|'ru'|'auto'
  Future<void> setConfig(Map<String, Object> cfg);
}
```

替代原 `speech_engine.dart`/`vosk_engine.dart`/`google_engine.dart` 三文件。无抽象基类（只剩一个引擎，YAGNI）。

### 7.2 `services/translation_router.dart`（新增，纯函数，必须可单测）

```dart
enum SrcLang { zh, en, ru }

class RouteResult { final SrcLang source; final String target; } // target: 'en'|'ru'|'zh'

/// 优先级：manualLang（手动 Chip，两种模式均有）> 字符集自动判定。
/// manualLang != null -> 直接按其定 source（不做字符集统计）。
/// 字符集统计：CJK(\u4e00-\u9fff等)、西里尔(\u0400-\u04FF)、拉丁(A-Za-z)
/// 主导比例 >= AsrConfig.charDominance(默认0.6) 判为该语言；
/// 混说句按主导字符集定方向（"我要book一个hotel" -> CJK 主导 -> zh）；
/// 全部模糊（纯数字/标点）-> 返回 null，调用方按 sticky 语言处理。
RouteResult? route(String finalText, String mode /* zhEn|zhRu */, {String? manualLang});
```

这是全 App 语言判定的**唯一**语义实现（Kotlin 侧 charMatch 只是同规则的机械兜底副本）。

### 7.3 `services/translation_service.dart`（改造）

接口：

```dart
class TranslationService {
  final http.Client _client;                       // 复用（Phase 1 成果）
  Future<String> translate(String source, String target, String text);      // 非流式（降级路径）
  Stream<String> translateStream(String source, String target, String text); // SSE delta 流
  void dispose();
}
```

`translateStream` 实现要点（Phase B）：
1. `http.StreamedRequest`/`Request` + `_client.send()`，body 带 `"stream": true`；
2. `resp.stream.transform(utf8.decoder).transform(const LineSplitter())` 逐行解析：`data: ` 前缀 → JSON → `choices[0].delta.content`；`data: [DONE]` 结束；
3. **首包超时 3s**：`Timer` 未收到首个 delta → 取消流 → 抛 `StreamTimeout`，调用方降级 `translate()`；
4. 请求级总超时 30s；异常统一转为用户可读错误；
5. 三家供应商（GLM/Qwen/Doubao）同为 OpenAI 兼容 SSE，共用一套解析；
6. **删除 `punctuate()`**——标点由翻译 Prompt 一并处理（S2/S6 消亡）。

Prompt 固定为附录 13.1 的同传模板（system），user 只放原文。`temperature: 0.2`。

### 7.4 `services/sentence_segmenter.dart`（新增）+ `services/tts_service.dart`（改造）

```dart
/// LLM token 永不直接进 TTS [INV-SENT]
class SentenceSegmenter {
  final void Function(String sentence) onSentence;
  void addDelta(String delta);  // 缓冲；遇 。！？.!?；;\n 或缓冲 >= 20 字 → flush
  void finish();                // 流结束冲刷余量
}

class TtsService {
  // TtsQueue：FIFO；speak 完成回调（awaitSpeakCompletion(true)）驱动下一条；
  // enqueue(text, lang)；stopAll() 清队列+停当前；
  // 队列自身不做语言过滤 —— 过滤在 LiveSession [INV-TTS]
}
```

### 7.5 `services/live_session.dart`（大幅精简重写）

保留：`SessionStatus` 枚举、`NoteEntry`、Selector 友好的不可变列表更新（`_notes = [..._notes, e]`）。
删除：Vosk 双模型仲裁、粘滞窗口、`_runPunct`、标点防抖 400ms、静音兜底 1200ms、字符数缓冲上限——**切句职责全部移交原生 VAD**。

核心编排（伪代码）：

```dart
void _onAsrEvent(AsrEvent e) {
  switch (e.type) {
    case 'partial': _partial = e.text; notifyListeners();        // 仅预览 [INV-PARTIAL]
    case 'final':   _partial = ''; _handleFinal(e.text);
    case 'error':   _setStatus(SessionStatus.error, e.text);
  }
}

Future<void> _handleFinal(String text) async {
  // manualLang：手动 Chip 状态（null=自动）。模式 A 仅作用于 Dart 路由；
  // 模式 B 同时下发 engine.setActiveLang（Kotlin 换活跃识别器）。
  final r = route(text, mode, manualLang: manualLang) ?? _stickyFallback(text);
  final entry = _appendPendingNote(r, text);                     // 先上原文行，译文流式填充
  final stream = settings.streamEnabled
      ? translator.translateStream(...) : null;
  if (r.source == SrcLang.zh) {
    // 面向对方：译文逐字上屏 + SentenceSegmenter -> TtsQueue [INV-TTS]
  } else {
    // 面向自己：中文大字幕逐字上屏，绝不接 TTS [INV-TTS]
  }
  // stream 首包超时/异常 -> translate() 整句降级，不崩、不静默丢句
}
```

`stop()` 顺序（Phase 1 成果保留）：先停引擎 → 取消未完成翻译流 → `tts.stopAll()` → 不再触发任何朗读。

### 7.6 `services/app_settings.dart`（字段增删）

| 字段 | 动作 |
|------|------|
| `engine`（vosk/google） | **删除**（连同设置页 UI） |
| `foreignLang`（en/ru） | 语义升级为 `mode`：`zhEn`/`zhRu`（沿用旧 key `foreign_lang` 做映射迁移：en→zhEn, ru→zhRu） |
| `modelBaseUrl` | **删除**（不再运行期下载模型） |
| `streamEnabled` | **新增** bool，默认 true（Phase B） |
| `llmProvider/llmApiKey/llmModel/ttsRate/fontSize` | 保留不动 |

### 7.7 UI

**`live_page.dart`**（保持 Selector 局部刷新结构）：
- 顶部：两枚模式卡片 `中⇄英` / `中⇄俄`（切换即 stop → start(newMode)）；
- 状态条：`SessionStatus`（加载模型中/聆听中/错误）；
- "正在听"预览区：partial 文本，弱化样式（灰色小字），明确不是结果；
- 主体：中文大字幕列表（`fontSize` 设置驱动）+ 同传气泡（你说的中文 + 外语译文，译文流式增长）；
- **两种模式均显示手动语种 Chip**（三枚：`自动`（默认）/ `中文` / `英文`或`俄语`，随模式切换文案）：
  - 模式 A：Chip 只作用于 Dart 侧 `route` 的 `manualLang`（单 bilingual 识别器无需通知 Kotlin）；
  - 模式 B：Chip 除设置 `manualLang` 外，还下发 `engine.setActiveLang('zh'|'ru')` 切换 Kotlin 活跃识别器；`自动` 档下发 `setActiveLang('auto')`（Phase A–C 内 auto 即维持 sticky，Phase D 才启用兜底重解码）；
- 底部：开始/停止大按钮。

**`settings_page.dart`**：删引擎选择与识别语种；加"流式翻译"开关；其余（供应商/Key/模型/语速/字号）保留。

---

## 八、关键数据流时序

### 8.1 你说中文（同传方向，模式 A）

```
说"这个多少钱" → VAD speechStart → bilingual partial："这个多" → UI 预览
→ VAD speechEnd(0.8s 静音) → final "这个多少钱" → route: CJK 主导 → zh→en
→ translateStream(zh,en) SSE："How" "much" "is" "this?" 逐 delta 上屏
→ SentenceSegmenter 攒到 "How much is this?"（遇 ?）→ TtsQueue → 手机朗读英语
```

### 8.2 对方说英语（字幕方向，模式 A）

```
"Where is the station" → partial 预览 → final → route: Latin 主导 → en→zh
→ translateStream(en,zh)："车站""在哪""里？" 中文大字幕逐字浮现
→ 不接 TTS，全程静音 [INV-TTS]
```

### 8.3 模式 B 语言切换（Phase D 自动兜底；Phase A–C 靠手动 Chip）

```
activeLang=zh，对方突然说俄语 → bilingual 输出乱码/空
→ final charMatch 失败 → ru 识别器重解码 SegmentBuffer（几百 ms）
→ 西里尔主导胜出 → emit final{text, langSwitched:"ru"} → activeLang=ru（sticky）
→ Dart 收到后正常走 ru→zh 字幕；UI Chip 同步高亮"俄语"
```

---

## 九、构建、CI 与工具链

### 9.1 Dart 3 升级（Phase A 第 1 步，一切的前提）

移除 vosk_flutter 后解锁。目标 `pubspec.yaml`：

```yaml
environment:
  sdk: '>=3.3.0 <4.0.0'          # Flutter 3.22+
dependencies:
  flutter: { sdk: flutter }
  # 不加 sherpa_onnx pub 依赖 —— 底座统一走 libs/ 里的官方 AAR（〇节清单 #1），避免双源
  http: ^1.2.0
  flutter_tts: ^4.0.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  permission_handler: ^11.3.0
# 删除：vosk_flutter / flutter_sound / speech_to_text
# 删除 assets 段的 3 个 vosk zip
```

升级伴随的 API 适配点：http 1.x（无破坏性影响，接口兼容）、flutter_tts 4.x（`awaitSpeakCompletion` 保留）、Dart 3 空安全已满足（现有代码已是 null-safe 风格）。

### 9.2 android/ 平台工程（Phase A 入库）

- `flutter create --platforms=android .` 生成后**提交入库**（Kotlin 自定义代码多，不能再靠 CI 现场生成）。
- `android/app/build.gradle` 关键配置：

```gradle
android {
  defaultConfig {
    minSdkVersion 23
    ndk { abiFilters "arm64-v8a" }
  }
  androidResources { noCompress "onnx" }
}
dependencies {
  implementation files("libs/sherpa-onnx-1.12.25.aar")   // GitHub Releases 直下入库，不走 Maven（见〇节清单 #1）
}
```

- Manifest：`RECORD_AUDIO` + `INTERNET`；**删除** Google RecognitionService `<queries>`。

### 9.3 CI 重写（codemagic.yaml + GitHub Actions）

流程统一为：

```
checkout → flutter 3.22+ (Dart 3) → flutter create 补 wrapper → git 恢复自定义文件
→ actions/cache 模型目录 → bash tools/fetch_models_github.sh
→ flutter pub get → flutter build apk --release --split-per-abi(可选)
→ 上传 APK artifact
```

- `flutter create` 仅用于生成 `gradle-wrapper.jar` 等二进制（文本工程文件已入库），随后 `git checkout --` 恢复自定义文件。
- `tools/fetch_models_github.sh`（**2026-07-31 起 CI/本地统一用此脚本**）：GitHub 官方 Releases 下载模型（CI 直连、本地开 VPN），已存在则跳过；AAR 已入库自动跳过。原 ModelScope 版 fetch_models.sh 因直链 404 弃用。
- BUILD.md 同步重写（环境要求 Flutter 3.22+、模型下载步骤、不再有"首启联网下载模型"）。

### 9.4 本地无 Flutter SDK 的约束

本开发沙箱无 Flutter SDK：所有验证靠静态审查 + CI 云端构建 + 用户真机试装。每个 Phase 完成的定义 = **CI 出包成功 + 用户真机验收通过**。

---

## 十、Phase 实施计划（文件级任务分解）

> 铁律：每个 Phase 独立可运行；完成即停，等用户确认再进下一个。Phase 内部按序号执行。

### Phase A：替换 ASR（不碰翻译逻辑）

| # | 任务 | 涉及文件 |
|---|------|----------|
| A-0 | **bilingual 模型真机验证**：先用 sherpa-onnx 官方 demo APK（或最小验证包）实测纯中/纯英/混说；不达标走 4.3 备选 | （无代码） |
| A-1 | pubspec 解锁 Dart 3、删旧依赖加新依赖、删 vosk assets 段；删除 `assets/*.zip` | pubspec.yaml, assets/ |
| A-2 | 生成并入库 android/ 平台工程；gradle 配置（minSdk/abi/noCompress/**libs/ AAR 依赖**）；AAR 下载入 `libs/` 并入库（〇节清单 #1）；Manifest 权限 | android/** |
| A-3 | 模型下载脚本 + `.gitignore` 模型目录（2026-07-31 定稿：`fetch_models_github.sh` 为唯一有效脚本；ModelScope 版 404 弃用） | tools/ |
| A-4 | Kotlin asr/ 7 文件落地（AsrPlugin/AudioSessionManager/AudioCapture/VadProcessor/RecognizerManager/SherpaRecognizer/AsrConfig）；模式 B 本阶段**只做手动 Chip**（charMatch 兜底重解码留给 Phase D，代码留桩） | android/.../asr/ |
| A-5 | Dart：新增 `native_asr_engine.dart`、`translation_router.dart`；删除 speech/ 旧三文件 | lib/services/ |
| A-6 | `live_session.dart` 精简重写（切句逻辑全删，编排 AsrEvent；翻译暂用现有**非流式** `translate`，改成 `(source,target,text)` 签名）；`app_settings.dart` 字段迁移 | lib/services/ |
| A-7 | `live_page.dart` 模式卡片 UI + 预览区 + **手动语种 Chip（两种模式均显示，见 7.7）**；`settings_page.dart` 删引擎项 | lib/pages/ |
| A-8 | CI 重写 + BUILD.md 更新 → 云端出包 → 用户真机安装 | codemagic.yaml, .github/, BUILD.md |

**验收（能用就行）**：连续说话不卡、UI 不掉帧（S3 消亡）；识别观感不低于 Vosk；模式切换正常；半小时长跑不崩不涨内存。

### Phase B：GLM Streaming 翻译（只动 TranslationService + LiveSession 接线，不碰 TTS）

| # | 任务 | 涉及文件 |
|---|------|----------|
| B-1 | `translateStream()` SSE 实现（7.3 全部要点：逐行解析/首包 3s 降级/总超时/多供应商共用） | translation_service.dart |
| B-2 | 删除 `punctuate()` 与 LiveSession 里 `_runPunct` 残余；Prompt 换 13.1 模板 | translation_service.dart, live_session.dart |
| B-3 | LiveSession 接流：pending note 译文逐 delta 更新（Selector 只刷新该条）；异常降级链路 | live_session.dart, live_page.dart |
| B-4 | 设置页加"流式翻译"开关（`streamEnabled`） | app_settings.dart, settings_page.dart |

**验收**：final 出文到首个译文字 <1s（观感明显变快）；关流式开关走整句；弱网/断网降级不崩不丢句。

### Phase C：TTS 队列（只动 TTS 链路）

| # | 任务 | 涉及文件 |
|---|------|----------|
| C-1 | `sentence_segmenter.dart` 新增（含单测友好的纯逻辑） | lib/services/ |
| C-2 | `tts_service.dart` 重写为 TtsQueue（FIFO + 完成回调驱动 + stopAll） | tts_service.dart |
| C-3 | LiveSession 落地 INV-TTS 过滤点：`source=zh` 译文流 → Segmenter → Queue；`en/ru` 永不接入 | live_session.dart |

**验收**：长句边译边播不破碎、不互相打断；**对方说英/俄语手机全程静音**；你说中文手机及时朗读；stop 后无残留朗读。

### Phase D（可选，"以后有空再玩"）

- D-1 模式 B 自动语言切换：启用 RecognizerManager 的 charMatch 兜底重解码（A-4 已留桩）+ `langSwitched` 事件接 UI Chip 同步。
- D-2 参数手感微调（VAD 阈值/静音时长/charDominance/Segmenter 阈值）。
- D-3 低端机降配开关、死代码清理、CI 回归。
- D-4 备选实验：若模式 A bilingual 纯英文偏弱，A/B 对比双 mono 方案。

---

## 十一、不变量清单（INV-*，任何后续修改不得违反）

| 编号 | 内容 |
|------|------|
| INV-TTS | 非对称朗读：只有 `source=zh` 的译文进 TTS（朗读外语）；`source=en/ru` 只出中文字幕、永远静音。过滤点在 LiveSession 分发处，TtsQueue 不做语言判断 |
| INV-PARTIAL | partial 只做"正在听"UI 预览；**final 才进翻译**。任何情况下不得把 partial 送入 LLM |
| INV-SENT | LLM token/delta 永不直接进 TTS；必须经 SentenceSegmenter 成句后入队 |
| INV-PROMPT | 翻译 Prompt 必须含同传约束（不解释/不总结/不增内容/口语化/容忍 ASR 小错），防书面化膨胀 |
| INV-OFFLINE | ASR 全离线，模型打进 APK；运行期不下载任何模型 |
| INV-BOUNDARY | Kotlin 只做采集/VAD/推理与机械字符规则；语义策略（翻译方向/供应商/TTS）只在 Dart |
| INV-STREAM-FALLBACK | 流式翻译必须带非流式降级路径，降级触发不允许丢句或崩溃 |
| INV-PRIORITY | 优先级冲突永远按 准确率>流式>实时>简洁 裁决 |
| INV-NOCAL | 不引入跨引擎置信度标定/融合公式/锁定状态机（v3.1 已否决的复杂度不复活） |
| INV-MANUAL | 手动语种 Chip 在**两种模式**下都存在且**优先级永远高于自动判定**（模式 A 作用于 Dart 路由，模式 B 还切 Kotlin 活跃识别器） |
| INV-MIRROR（2026-07-31 修订） | AAR 只用 `libs/` 入库版本，gradle 不得新增 Maven 在线拉取；模型统一经 `tools/fetch_models_github.sh`（GitHub 官方源）下载——CI 海外 runner 直连、本地开 VPN，均已实测。**本地国内裸网环境不得指望任何在线拉取**；ModelScope 直链未经 curl 验证不得写入脚本（fetch_models.sh 的 404 教训） |

---

## 十二、风险与回退预案

| 风险 | 概率 | 预案 |
|------|------|------|
| bilingual 模型纯英文识别不达标 | 中 | 4.3 备选：zh+en 双 mono + sticky 兜底（与模式 B 同构，代码复用）；决策点在 A-0，不影响后续 Phase |
| ~~sherpa AAR Maven 国内拉取失败~~ | 已消除 | v1.1 起 AAR 直下入 `libs/` 并入库、模型只走 ModelScope（〇节清单），构建全程不依赖 GitHub/Maven 网络 |
| Dart 3 升级连带 API 破坏 | 低 | 依赖都选主流稳定版；升级独立为 A-1 一次提交，CI 先验编译再继续 |
| 俄语 T-one CTC 集成接口与 transducer 不一致 | 中 | SherpaRecognizer 统一包装差异；Phase A 模式 B 仅手动 Chip，风险窗口小 |
| VAD 切句手感差（截断/迟钝） | 中 | 参数全部在 AsrConfig 可热调（D-2）；minSilence 0.8s 起步偏保守 |
| SSE 在个别供应商上格式差异 | 低 | 三家均 OpenAI 兼容；解析器容忍空行/注释行/`[DONE]` 缺失（以连接关闭兜底） |
| APK 体积 ~370–390MB 接受度 | — | 2026-07 二次修正：bilingual int8 ~190MB + 俄语仅 fp32 ~138MB；单 abi 已是最省；缓解：bilingual 降级 small 版（int8 ~48MB）、俄语自行量化 int8 或改按需下载 |

回退总原则：每个 Phase 单独分支/标签提交，验收不过整体 revert 该 Phase，不留半成品。

---

## 十三、附录

### 13.1 同传翻译 Prompt（system，`{target}` 运行期替换）

```
你是实时口语翻译引擎。
任务：将输入内容直接翻译为目标语言（{target}）。
要求：
1. 保留原意
2. 使用自然口语
3. 不解释
4. 不总结
5. 不添加不存在的信息
6. 输入可能来自语音识别，允许少量错误，按最合理口语意图翻译
只输出译文本身。
```

### 13.2 EventChannel 上行事件协议（JSON）

```json
{ "type": "ready" }                                  // 模型加载完成
{ "type": "vadStart" }
{ "type": "vadEnd" }
{ "type": "partial", "text": "这个多" }
{ "type": "final",   "text": "这个多少钱",
  "langSwitched": "ru" }                             // 可选，仅模式B自动兜底切换时携带
{ "type": "error",   "text": "AudioRecord read failed(-3)" }
```

MethodChannel 下行：`start({mode})` / `stop()` / `setActiveLang({lang})` / `setConfig({json})`。

### 13.3 AsrConfig 参数表（Kotlin 持有，Dart 可热更）

| 参数 | 默认 | 说明 |
|------|------|------|
| vadThreshold | 0.5 | Silero 语音概率阈值 |
| minSilenceMs | 800 | 静音多久判句子结束 |
| minSpeechMs | 250 | 语音多短判噪声丢弃 |
| frameMs | 100 | 采集帧长 |
| segmentBufferMaxSec | 15 | 模式 B 兜底重解码缓冲上限 |
| charDominance | 0.6 | 字符集"主导"判定比例（Dart route 与 Kotlin charMatch 同值） |
| segFlushChars | 20 | SentenceSegmenter 无标点强制切句长度（Dart 侧） |
| firstTokenTimeoutSec | 3 | SSE 首包降级超时（Dart 侧） |

### 13.4 与历史文档的关系

- 《Project Review.md》：问题编号（S/M/L）与 Phase 1 修复记录的出处，不再更新。
- 《架构升级提案_Sherpa-ONNX路径.md》《整体重构方案_SherpaONNX.md》：五轮评审的决策过程存档；结论已全部吸收进本文件。
- **今后动码只看本文件**；若实施中发现本文件与现实冲突（如库 API 变动），先改本文件再改代码。

---

**下一步**：确认本规划后，从 **Phase A（A-0 bilingual 模型真机验证 + A-1 Dart 3 解锁）** 开始动码。
